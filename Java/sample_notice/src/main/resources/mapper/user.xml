<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nts.intern.dao.UserDao">

    <select id="exists" parameterType="com.nts.intern.vo.User" resultType="boolean">
		SELECT EXISTS(
		SELECT id
		FROM user
		WHERE id = #{id}
		AND passwd = #{passwd});
	</select>

</mapper> 